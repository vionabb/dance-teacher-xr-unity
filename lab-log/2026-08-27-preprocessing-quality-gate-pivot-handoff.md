---
date: 2026-09-05
tags: [pose-preprocessing, validation, participant-data, motion-pipeline, human-annotation, handoff]
artifacts: []
---

# Handoff: corpus-wide quality triage + defect localization

**This file is a living document, not a dated narrative entry.** Maintain it in place across sessions — rewrite the status/next-action sections as work progresses rather than appending a history of them, and prune narrative once its outcome is captured below or in git history. It is the child/companion plan for [2026-08-27-preprocessing-quality-gate-pivot.md](2026-08-27-preprocessing-quality-gate-pivot.md); read that entry first for the "why." **If another session is active on this same branch/worktree, treat this doc as shared state**: check `git log`/recent commits before rewriting, and reconcile rather than clobber a section another session just changed.

## Status (as of 2026-09-05)

- **Phase 1 (pose extraction): complete.** 1,808/1,808 clips (52 reference + 675 study1 + 1,081 study2), canonical `pose2d`+`pose3d` via GPU, zero errors.
- **`quality_triage`: 60/60 complete**, under annotator `viona`. (An annotator-identity split — a typo login `vioina` holding the real 60/60 progress, separate from the correctly-spelled `viona` — was found 2026-09-01 and resolved 2026-09-02 by merging `vioina`'s completed judgments into `viona`; see commit `6038205` if the mechanics are ever needed again.)
- **`error_marking`: 1/17 pilot tasks touched.** `error-marking-009` is in progress (autosaving) under `viona`; the other 16 not started. Its 4 existing marks (on the now-retired `left_arm`/`right_arm` ids) are untouched and still display correctly under the new per-landmark default list — see the skeleton-overlay entry below.
- **`video_quality_rating`: 0/20 pilot tasks done.**
- The `error_marking` screen went through a full timeline-driven UI redesign (2026-09-01 through 2026-09-03, many commits — see `git log` on this branch for the blow-by-blow). Current interaction model: one row per body part; create a mark by click-dragging a row, clicking a per-row **+** button at the current playhead frame, or dragging a mark's edge; click a mark to set cause(s)+note in a popup; segments colored by cause with a legend; horizontal scroll for long clips; inline body-part list editing; a playhead line; a frame scrubber (thin line + dot) with `‹`/`›` (±1 frame) and `‹⁵`/`⁵›` (±5 frame) step buttons.
- **Interactive skeleton overlay landed on top of the timeline (2026-09-03, commit `1a54110`), merging what had been two separate concepts (`error_marking`'s timeline and the single-frame skeleton editor's drag/occlusion pattern) into one screen.** The tracked skeleton now renders as an SVG over the video, synced to the current frame and color-coded to the same cause-color legend as the timeline. Clicking a landmark creates/extends a mark for that frame (merging into an adjacent existing mark instead of fragmenting, via a shared `ensureMarkAtFrame()` also now used by the timeline's + button); dragging a landmark does the same and additionally records the dropped-at position as that frame's correction (a new sparse per-mark `positions` field, frame → `[x, y]`). The body-part vocabulary switched from 8 coarse regions to the 12 individually-tracked landmark names (matching what the overlay actually draws) via a non-destructive migration — see the schema section below. New: `attach_error_marking_landmarks.py` backfills the per-frame landmark pixel coordinates every `error_marking` task needs for this (recomputed from the same automatic-signals CSV and `_pose_pixels()` the burned-in overlay was originally rendered with, no new pose estimation); it's now wired into `append_error_marking_tasks.py` via `--signals-csv` so future batches get it automatically, and has already backfilled the live 20260828 batch (17/17). **The server was restarted 2026-09-03 to pick this up** (manifest + `server.py` changes only take effect at process start) and confirmed serving schema 3.6, the landmark-based defaults, and all 17 tasks' `landmarks_artifact` — `error-marking-009`'s 4 in-progress marks (still under the old `left_arm`/`right_arm` ids) came through the restart untouched, verified directly against the running server's `/api/state`.
- **Overlay/video frame mismatch found and fixed (2026-09-04, commit `38be569`, no restart needed — static files only).** Viona reported the skeleton overlay landing a frame off from the burned-in video. Root cause was two compounding bugs, both invisible until the overlay needed the reported frame to exactly match decoded video content, not just be "close enough" for a human eyeballing a scrubber: (1) seeking to exactly `frame/fps` lands on the boundary between that frame and its neighbor, which doesn't always round the intended way — fixed by seeking to each frame's midpoint (`frameToTime()`); (2) `video.currentTime` assignment is asynchronous in every browser (not just Safari), so `errorMarkingCurrentFrame()` re-deriving the frame from a *fresh* read of it — as it used to — wasn't reliably caught up yet, even once the on-screen frame label already showed the right number. Fixed by tracking the frame as real state (`state.errorMarkingFrame` / `setErrorMarkingFrame()`), set explicitly at every deliberate seek, reconciled from the actually-decoded position only by `video.ontimeupdate`. Verified the landmark *data* was already pixel-correct before touching client code, by extracting the burned-in dot pixels directly from a real `clip.mp4` and diffing them against `landmarks.json`. New Playwright coverage (a full-clip seek round-trip, and an adjacent-frame click test) confirmed failing against the pre-fix code and passing after, by reverting the fix locally first. Also relocated the frame counter above the video and replaced the step buttons' `<sub>5</sub>` markup with a fixed-size `<span>`, which is the likely cause of the Safari `+`-button-pair line-wrap the other session's earlier fix didn't fully resolve.
- Corpus-wide **frame-quality map** (one pixel row per clip, one column per frame, colored white/yellow/red by defect severity) now supports `--corpora reference chi25_study1 chi25_study2` (added 2026-09-03) to include the reference corpus; reference clips land in an "Other / unparsed" group since their filenames don't match the participant `userstudyN-<dance>-<condition>` pattern the dance-grouper expects. **Extended further the same day, per Viona's explicit direction that this map is about input signal quality, not about validating any correction technique** (see "Decisions" below): (1) reconciled `select_suspicious_frames.py`'s and `compute_automatic_quality_signals.py`'s two independent "is tracking implausible?" velocity/visibility computations into one shared `landmark_velocity_and_visibility()`, verified numerically identical on 60 real clips; (2) `_oob_severity` (the yellow/red "is this fixable?" classifier) and `false_tracking_signal` now carry explicit REGENERATE callouts, `run_provenance.json` records the git commit and a hash of the detection/fixability source, and `temp/experiments/latest-frame-quality-map` is a symlink to the most recent run that a rerun compares itself against and prints a one-line note if the code changed; (3) each row now also carries three small severity-indicator swatches -- crop and lighting (automatic, every row, reusing `compute_automatic_quality_signals.py`'s `crop_signal`/`lighting_signal`) and clothing (from a completed `video_quality_rating` judgment -- sparse, 42/1808 clip-rows, all Claude's own pilot reference ratings since Viona hasn't rated the 20-video pilot yet) -- plus full numeric columns in the CSV, meant for spotting patterns between input video quality and error prevalence. Latest run (all three): `temp/experiments/20260903-151831-frame-quality-map/` (not committed — `temp/` is gitignored; `uv run python -m motion_extraction.scripts.render_corpus_frame_quality_map --corpora reference chi25_study1 chi25_study2` reproduces it).
- **Automated test coverage for `error_marking` added 2026-09-03** (previously none at all, despite real interaction complexity — drag state machines, event delegation, client-side migrations): server-side validation/persistence tests for the response schema and the new `positions` field, Playwright coverage for the overlay's SVG hit-test/drag-threshold math and adjacent-mark-merging, and a synthetic-pose-CSV test proving the landmark backfill reproduces the original render step's exact window and pixels.
- **Mark-detail popup now shows the corrected skeleton overlay with the attributed landmark highlighted (2026-09-04), and the `error_marking` screen got a UI pass for mobile/review workflow (2026-09-05, no restart needed — static files only):** the skeleton-adjustment canvas/video gets much more of the viewport on mobile (`#editor-viewport`/`#error-marking-video` at 82vh/58vh); one Replay control now sits inline with the frame-step/scrubber controls, advances through every frame at 2fps, becomes Pause during playback, and resumes from the paused or scrubbed frame (the review dialog uses the same single-speed control); resizing or drawing a span until it touches another span for the same body part now coalesces them while preserving causes, corrected positions, and notes; "Complete case" is relabeled "Done annotating" and, for an `error_marking` task with edits since the last review (`state.errorMarkingDirty`), now opens a full-screen-on-mobile review dialog that autoplays the clip with "Edit annotations"/"Looks good" (only "Looks good" actually completes); every screen's instructions are now a collapsed-by-default `<details>`. Replay/Pause was verified live against the running server; the focused annotation tests and full motion-pipeline acceptance gate pass. The earlier live review-gate check left one throwaway completed case under a `claude-preview` test annotator in the local `annotations.sqlite3` (gitignored, harmless, doesn't touch `viona`'s progress); delete that row or ignore it.

## Next actions

1. Pick up `error-marking-009` (in progress under `viona`) and continue through the 17-task `error_marking` pilot batch.
2. Rate the 20-video `video_quality_rating` pilot (Claude's own ratings for the same 20 are already recorded under annotator `claude`, for an agreement comparison once Viona's are in).
3. Spot-check the one clip with `finite_coordinate_fraction == 0` (fully undetected throughout, from the automatic-signals sweep) — genuinely bad clip, or a pipeline issue?
4. Decide whether to send the drafted MediaPipe GPU-memory-leak bug report (`/feedback`).
5. Once real `error_marking` ground truth exists: revisit whether occlusion errors are detectable from pose data (one naive proxy already failed — see "Decisions" below) against real per-frame labels instead of eyeballing one clip.
6. Once more `video_quality_rating` judgments exist (see action 2) and/or the crop/lighting indicators have run against a meaningfully-sized batch: actually do the crop/lighting/clothing-vs-error-prevalence pattern analysis the frame-quality map's row indicators were built for (2026-09-03), using `frame_quality_summary.csv`'s indicator columns against its `red_fraction`/`badness` columns — this hasn't been done yet, only the plumbing to make it possible has.
7. Regenerate the frame-quality map after any future change to detection ("is tracking implausible?") or fixability-classification ("is this tracking error fixable?") code, per the REGENERATE callouts in `select_suspicious_frames.py`/`render_corpus_frame_quality_map.py`/`compute_automatic_quality_signals.py` — this is now a standing habit Viona wants going forward, not a one-time request.

## How to resume

**Runtime status (2026-09-05)**: the branch now uses the original checkout, the former linked worktree has been removed, and the annotation server is stopped. Before removal, the ignored annotation database and experiment artifacts were migrated and verified; the database passed its integrity check and a byte-identical backup was retained locally. Locate these ignored runtime files locally before restarting, and do not record machine-specific paths, network details, access codes, or other secrets in this handoff.

**Future-worktree data gotcha**: ignored research data and generated experiment outputs are not shared automatically between checkouts. Before removing a future worktree, inventory and migrate its unique ignored files. Avoid broad Git staging in a worktree containing dataset symlinks, because those symlinks can be staged even when their target directories are ignored.

Use the existing task-generation and landmark-attachment scripts when expanding the annotation batch; inspect their current help and launch examples for arguments rather than preserving machine-local invocations here.

## `error_marking` / `video_quality_rating` schema

`error_marking_response_json` (schema 3.6, 2026-09-03) = `{"marks": [{"body_part": str, "start_frame": int, "end_frame": int, "causes": [str], "note": str, "positions": {str(frame): [x, y]}}], "no_errors_found": bool, "note": str}` — validated in `server.py`'s `_validate_error_marking_response`/`_validate_mark_positions`. `positions` is new: a sparse map from frame (as a string key, since JSON object keys are strings) to a corrected `[x, y]` pixel position, populated only for frames the annotator actually dragged the landmark on via the skeleton overlay; each entry's frame must fall inside that mark's own `[start_frame, end_frame]`. `body_part`/`causes` are still open vocabularies (any string ≤64 chars), not closed enums, per Viona's request — editable in the UI, persisted in `localStorage` per-device (seeded defaults in both `server.py`'s `DEFAULT_ERROR_BODY_PARTS`/`DEFAULT_ERROR_CAUSES` and `app.js`'s `FALLBACK_ERROR_LISTS` — keep in sync by hand). `DEFAULT_ERROR_BODY_PARTS` is now the 12 tracked landmark names (`LEFT_SHOULDER`, `RIGHT_SHOULDER`, `LEFT_ELBOW`, `RIGHT_ELBOW`, `LEFT_WRIST`, `RIGHT_WRIST`, `LEFT_HIP`, `RIGHT_HIP`, `LEFT_KNEE`, `RIGHT_KNEE`, `LEFT_ANKLE`, `RIGHT_ANKLE` — the same set `POSE_EDGES` in `run_preprocessing_experiment.py` draws), replacing the old 8 coarse regions; `DEFAULT_ERROR_CAUSES` is now `occlusion`/`motion_blur`/`background_confusion`/`suboptimal_clothing` (Viona's simplification request), replacing `out_of_frame`/`missing_tracking`/`other`. Both switches are non-destructive on the client: `migrateBodyPartDefaults()`/`migrateCauseDefaults()` in `app.js` only replace a device's stored list when it's still exactly the prior shipped default, wholesale, preserving any custom entries and leaving an already-customized list untouched; already-submitted marks keep their old `body_part`/`cause` ids regardless and keep displaying (an id not in the current list just renders as its own unlabeled-but-functional timeline row). Frame indices are local to the rendered `clip.mp4` (frame 0 = first frame of that window), not the original source video.

An `error_marking` task now also carries `landmarks_artifact` (path to a per-frame `{landmarks, pose_edges, source_dimensions, source_window, frames: [{LANDMARK: [x, y]}, ...]}` JSON, relative to the experiment root) and `source_dimensions` (`{width, height}` of the original video), both written by `attach_error_marking_landmarks.py`. A task without `landmarks_artifact` (e.g. one from before 2026-09-03, not yet backfilled) just shows the video with no overlay — the rest of the screen works unchanged.

`quality_rating_response_json` = `{"lighting": str, "clothing": str, "note": str}`, both required on completion.

## Decisions already made (do not re-litigate without asking Viona)

- **Sampling**: automatic-flagged candidates plus random unflagged controls, so labels can validate both precision and recall of any detector.
- **Overlay always on**: every review item shows the skeleton overlay burned in, never raw video/frame alone — video quality and pose quality are independent judgments Viona wants visible together.
- **Cropping** is detected from hip/shoulder landmark position relative to frame bounds (Viona's own suggestion). **Hip out-of-frame is normal waist-up webcam framing** (18% of all frames corpus-wide) and is not treated as a defect on its own — any framing signal should weight/gate on the 6 arm/shoulder landmarks, not hips. Landmark `_vis` is not uniformly trustworthy as an out-of-frame proxy: wrist `_vis` is reasonably trustworthy (~5.5–8.7% false-confident); hip/shoulder `_vis` are not (~50–52%/~95–98%).
- **Occlusion-error detection from pose data alone is an open problem**, not solved or disproven — one naive wrist/shoulder-midline-crossing proxy failed (didn't discriminate on `quality-triage-033`). Don't build another automatic occlusion detector without validating against real per-frame `error_marking` labels first.
- The originally-planned Stage 2 (`defect_localization`, single warm-started span+cause) is **superseded by `error_marking`** (typed, multi-span, extensible vocabulary) — don't build `append_defect_localization_tasks.py` as originally scoped.
- **"Right hip"/"Left hip" are merged into "Hips"; "Torso" is relabeled "Shoulders"** (2026-09-01, Viona's request, migration-safe). Don't reintroduce separate hip sides without her asking again.
- **`error_marking` body parts switched from 8 coarse regions to the 12 individually-tracked landmarks, and causes simplified to occlusion/motion_blur/background_confusion/suboptimal_clothing** (2026-09-03, Viona's request, both migration-safe — see the schema section above). Don't reintroduce the coarse regions or the old out_of_frame/missing_tracking/other causes without her asking again.
- The error-marking timeline's event listeners are **delegated to the stable `#error-marking-timeline` container**, never bound to individual segments/tracks/handles (those get destroyed/recreated on nearly every interaction). Keep this pattern for further additions — see `attachTimelineHandlers()` in `app.js`.
- A per-mark `note` exists alongside the whole-clip note — they answer different questions, don't conflate them.
- **CHI25's own 8 upper-body landmarks** (`LEFT`/`RIGHT_SHOULDER`, `_ELBOW`, `_WRIST`, `_HIP`) are the reference landmark set for framing analysis, matching what the thesis's automatic rating used — not all 33 MediaPipe landmarks.
- **The frame-quality map is a input-signal-quality/detection-heuristic view, not a correction-effectiveness validator** (2026-09-03, Viona's explicit direction). Its yellow/red classification means "the detector thinks this is correctable," not "a correction technique fixes it" — the script never runs pose data through `PosePreprocessingConfig` or any other correction pipeline. Regenerate it as detection ("is tracking implausible?") or fixability-classification ("is this tracking error fixable?") code changes, per the REGENERATE callouts on `_oob_severity`/`false_tracking_signal`, so its "program's view of the data" stays current with that code — but don't extend it to run correction techniques and compare their output; that's a different, not-yet-built validation against real `error_marking` ground truth.

## Reference numbers (full 1,808-clip corpus, canonical GPU-extracted pose2d)

| signal | median | 90th pct | notes |
| --- | --- | --- | --- |
| `crop_violation_fraction` | 0.05 | 1.00 | Reference corpus mean 0.004 (near-zero, professionally framed) vs. study1 0.24 / study2 0.42 (self-recorded) — validates the detector is tracking something real. |
| `normalized_acceleration_p95` | 1.07 | 1.74 | Needs fresh human-label comparison; thresholds don't carry over from the pre-GPU-model sweep. |
| `false_tracking_candidate_fraction` | 0.39 | 0.64 | Still needs calibration against real human judgment on this corpus — not a validated cutoff. |

Out-of-frame exclusion isn't viable as a blanket filter: 59.3% of clips have *some* out-of-frame CHI25 landmark. It's overwhelmingly a **hip** problem (18% of frames), not arms (wrists 9–10%, elbows 1.6%, shoulders <0.3%).

Frame-quality map (white=clean / yellow=correctable / red=uncorrectable): latest run (reference included, plus crop/lighting/clothing row indicators) at `temp/experiments/20260903-151831-frame-quality-map/`; the prior study-only run (2026-08-31, no indicators) was 63.9%/28.5%/7.6% frame-weighted, with 17/1,756 clips more than half red.

## Context

The 2026-08-27 pivot ([2026-08-27-preprocessing-quality-gate-pivot.md](2026-08-27-preprocessing-quality-gate-pivot.md)) paused smoothing-parameter work because source-video and pose-detection quality issues dominate the corpus and confound any smoothing comparison. This plan built a purpose-built annotation workflow to gather ground truth across the full corpus rather than relying on ~30 pre-existing, non-systematic human labels.

Committed on `worktree-preprocessing-quality-triage-handoff` (pushed to origin, no PR opened yet). Earlier versions of this document carried a full narrative — out-of-frame/hallucination methodology, the original single-`error_type` schema, a six-commit timeline-redesign walkthrough, the original Stage 2 architecture writeup — that has since settled into the "Decisions" and "Reference numbers" sections above and been trimmed. If the reasoning behind a specific past decision is needed, `git log -p -- lab-log/2026-08-27-preprocessing-quality-gate-pivot-handoff.md` has it.
