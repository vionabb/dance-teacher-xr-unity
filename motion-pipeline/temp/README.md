# Temporary pipeline outputs

Put generated smoke-test and pipeline outputs here when they should not be committed. The committed shared smoke-test inputs live under `../data/test-fixtures/smoketest/` and must not be used as an output directory.
